---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: frekele/gradle

inputs:
  - name: git-repo
  - name: gradle-project
  - name: db
outputs:
  - name: out

run:
  path: ./git-repo/gradle-test.sh
